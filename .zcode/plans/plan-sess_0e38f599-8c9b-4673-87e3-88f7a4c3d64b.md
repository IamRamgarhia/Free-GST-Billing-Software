## Audit complete — deliverable: one consolidated report file (no code changes)

**What I did:** Read every source file in the repo line-by-line (via 3 parallel audit passes over: ① server.js + store + App + utils + Google Drive + configs, ② InvoiceGenerator/PurchaseBills/InvoicePreview/Clients/OCR/Inventory/Receipts/Recurring, ③ GSTReturns/IncomeTax/itr.js/Dashboard/Reports/Settings/PrintSettings + all scripts/bat/ps1/hta release tooling). All `.md` files and old reports were ignored as requested. **~120 distinct findings** with file:line references were collected.

**What I will do next (single step):** Write a brand-new report to `docs/AUDIT_2026-08-23.md` consolidating all findings, organized by severity and category, each with file:line evidence. No source code will be touched.

### Report structure
1. **Executive summary** — severity counts, top-10 must-fix list
2. **Critical bugs** (3) — e.g. registered credit notes double-subtracted from GSTR-1/3B grand totals (GSTReturns.jsx:594-726); Income-Tax FY constants stale *and* contradictory between UI (2024-25) and engine (2025-26) while real FY is 2026-27, silently dropping bills from ITR prefill
3. **High bugs (~20)** — wrong financial year in every invoice number minted Jan–Mar (store.js:144-148 — GST compliance issue); stock deduction wrong for duplicate lines/credit notes/challans; recurring-template edit wipes most template fields; purchase-bill stock double-count on rename + delete flow that promises stock reversal which doesn't exist; ClientsView changeStatus fabricates paidAmount and overwrites bills with stale payments (payment-history corruption); orphaned receipts resurrect deleted payments on Dashboard; thermal receipts print wrong currency & ignore discounts so rows don't sum to total; ReportsView P&L counts proformas/challans as revenue and credit notes as positive
4. **Security findings (~13)** — updater downloads the `main`-branch zip with no checksum and runs `npm install` (postinstall = RCE on account compromise), remotely triggerable via registered `freegstbill-update://` protocol; Node MSI installed with no integrity check; "SHA-256 verified" traineddata claim is false (size-check only); no auth on server (localhost-origin-only model); restore expands arbitrary ZIPs unvalidated; PS command built by string interpolation in build script
5. **Data-loss / corruption risks** — recurring auto-fire can overwrite existing bills bypassing the 409 dupe guard; draft silently destroyed on Back without client name; sidebar nav bypasses the leave-guard; client-credit dual save is non-transactional; export omits trash; restore serves stale cache
6. **Unwired / dead features (~25)** — 4 Print-Settings format controls that do nothing; OCR tax breakdown parsed then discarded; recurring form fields with no inputs; `reminderEnabled` never read; compute234AInterest never called; dead endpoint /api/health; half-wired command palette; onEdit prop passed to a component that takes no props
7. **Over-engineering / duplication (~20)** — two divergent recurring-invoice generators (client vs server); tax-split aggregation re-implemented 5× inside GSTReturns; period filter copy-pasted across 4 views with diverging semantics; ~200 lines of copy-paste CRUD ×10 on server and client; print-template snapshots nest recursively (exponential localStorage growth); 3 overlapping service-worker update mechanisms
8. **Performance** — full bill list loaded per invoice; O(n²) stats per keystroke in ClientsView; unmemoized render-path JSON parsing in hot components; notification refetch on every view change; no list virtualization
9. **Script/build tooling bugs** — Update .bat kills wrong port (stale variable expansion); npm install/build exit codes unchecked so "Update Complete!" prints after failures; fallback opens dead port 3001; Unix has no stop script though the Control Panel shows the button
10. **UX / validation / accessibility gaps** + **prioritized improvement roadmap**

Each finding will carry: severity, `file:line`, description, and code evidence — exactly as gathered; nothing copied from the old reports.

**Out of scope (per your instruction):** any code modification, any reading/copying of existing .md reports/docs.